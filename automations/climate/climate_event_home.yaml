---
# Turn climate on when receiving push notification event

alias: "climate_event_home"
trigger:
  - platform: event
    event_type: "ios.notification_action_fired"
    event_data:
      actionName: CLIMATE_HOME
action:
  # Turn heating on
  - service: script.climate_home
  # Notify
  - service: script.push_processing
    data_template:
      title: "Thermostat 🌡️"
      message: "Le thermostat est reglé sur {{ states.climate.living.attributes.temperature | int}}°"
      identifier: "climate_main"
      category: "climate_off"
